import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    headerZoneTotal: {
        height: 50,
        flex: 3,
        flexDirection: 'row',
        justifyContent: 'flex-end',
        borderWidth: 1,
    },
})